version: 2.1
jobs:
  first_job:
   docker:
      - image: circle/node:14.17
   steps:
      - run:
          name: Npm Lib
          command: npm install
      - run:
          name: build docker
          command: docker build -t demo_image .
  
workflows:
  version: 2
  build:
    jobs:
      - first_job          
